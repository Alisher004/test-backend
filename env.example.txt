# Скопируйте этот файл в .env и заполните значения
# cp env.example.txt .env

# Server Configuration
PORT=5001
NODE_ENV=development

# MongoDB Configuration (замените PostgreSQL на MongoDB)
MONGO_URI=mongodb://localhost:27017/okurmen_test
# Для production используйте:
# MONGO_URI=mongodb://username:password@host:27017/database?authSource=admin

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production

# CORS Configuration (опционально)
# "*" или "all" - разрешить все origins (по умолчанию)
# Или укажите конкретные домены через запятую: https://domain1.com,https://domain2.com
ALLOWED_ORIGINS=*

